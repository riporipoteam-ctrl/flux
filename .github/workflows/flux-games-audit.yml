name: Flux Games Audit

on:
  pull_request:
    paths:
      - "src/app/(main)/games/**"
      - "src/app/games/play/**"
      - "src/components/game/**"
      - "src/data/browser-games.ts"
      - "src/data/flux-arcade-games.ts"
      - "src/services/game-leaderboards.ts"
      - "src/styles/flux-x-ultimate.css"
      - "src/app/layout.tsx"
      - "src/app/(main)/home/page.tsx"
      - "scripts/flux-x-games-v3-audit.mjs"
      - ".github/workflows/flux-games-audit.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      GITHUB_PAGES: "true"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: node scripts/flux-x-games-v3-audit.mjs
      - run: npm run lint
      - name: Exclude server-only routes from static export
        run: mv src/app/api .pages-api
      - run: npm run build
      - name: Verify static game routes
        run: |
          test -f out/games/index.html
          test -f out/games/play/index.html
          test -f out/games/arcade/index.html
          test -f out/games/flux-farm/index.html
