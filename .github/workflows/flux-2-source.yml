name: Flux 2 source snapshot

on:
  push:
    branches: [feat/flux-2-big-update]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snapshot:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Upload source
        uses: actions/upload-artifact@v4
        with:
          name: flux-2-source
          path: |
            .
            !.git/**
            !node_modules/**
            !.next/**
            !out/**
          include-hidden-files: true
          retention-days: 1
